<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Moti" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/mt_main</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Moti" parent="Base.Theme.Moti" />

    <style name="category_toggle">
        <item name="thumbTint">@color/white</item>
        <item name="track">@drawable/style_toggle_btn</item>
        <item name="android:thumb">@drawable/style_toggle_thumb</item>
        <item name="android:enabled">true</item>
    </style>
</resources>